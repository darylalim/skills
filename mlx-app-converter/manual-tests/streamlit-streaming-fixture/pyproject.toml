[project]
name = "streamlit-streaming-fixture"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "streamlit",
    "transformers",
    "torch",
]

[dependency-groups]
dev = [
    "pytest",
    "ruff",
    "ty",
]
